import React, { useContext } from 'react';
import PropTypes from 'prop-types';

const propTypes = {};

export const Main = () => {
  return (
    <div>
      <span>Testss</span>
    </div>
  );
};

Main.propTypes = propTypes;

export default Main;
